import React from 'react';

import './react-lib.scss';

/* eslint-disable-next-line */
export interface ReactLibProps {}

export const ReactLib = (props: ReactLibProps) => {
  return (
    <div>
      <h1>Welcome to react-lib!</h1>
    </div>
  );
};

export default ReactLib;
